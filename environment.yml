name: sp

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python >= 3.8
  - pip
  - setuptools_scm
  - toml

  - pytask >=0.2
  - pytask-latex >=0.2
  - pytask-parallel >=0.2

  # Dependencies
  - black
  - dask
  - fastparquet
  - jupyterlab
  - pandas
  - python-graphviz
  - python-snappy
  - scikit-learn
  - beautifulsoup4
  - matplotlib
  - pre-commit
  - pytest
  - pytest-cov
  - tox
  - tqdm
  - openpyxl

  - pip:
    - -e .
