name: sp

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python >= 3.8
  - pip
  - setuptools_scm
  - toml

  - pytask
  - pytask-latex
  - pytask-parallel

  # Dependencies
  - black
  - dask
  - fastparquet
  - jupyterlab
  - pandas
  - python-graphviz
  - python-snappy
  - scikit-learn
  - beautifulsoup4
  - matplotlib
  - pre-commit
  - pytest
  - pytest-cov
  - tox
  - tqdm
  - openpyxl

  - pip:
    - -e .
